#include<iostream>
#include<string>
#include<vector>

using std::cin;
using std::cout;
using std::endl;
using std::string;
using std::vector;

int main() {
    // int i;
    string i;
    // vector<int> ivec;
    vector<string> svec;

    while (cin >> i) 
        // ivec.push_back(i);
        svec.push_back(i);
    
    return 0;
}